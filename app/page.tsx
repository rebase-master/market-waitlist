// Placeholder shell so the scaffold builds and renders on-brand. The full
// waitlist page (hero + form island + trust strip + FAQ) lands in a later chunk.
export default function Home() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-md flex-col items-center justify-center px-6 text-center">
      <span className="font-display text-2xl font-bold tracking-tight">Amanah</span>
      <h1 className="mt-4 font-display text-3xl font-bold leading-tight text-balance">
        Halal financing, built for Egypt.
      </h1>
      <p className="mt-3 text-ink-muted">
        No riba. No hidden fees. The waitlist opens here soon.
      </p>
    </main>
  )
}
